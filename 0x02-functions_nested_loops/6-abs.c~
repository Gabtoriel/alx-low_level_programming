#include "main.h"

/**
 * _abs - function to print the absolute value of given number
 * @n: number to return its absolute value
 *
 * Return: the absolute value of the number (int)
 */
int _abs(int n)
{
	int abs = (int) n;
	return (abs);
}
