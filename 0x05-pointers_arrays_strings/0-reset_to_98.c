#include "main.h"

/**
 * reset_to_98 - this uses a pointer to change the value of a global variable
 * @n: a pointer to the global variable passed to the function
 */
void reset_to_98(int *n)
{
	*n = 98;
}
